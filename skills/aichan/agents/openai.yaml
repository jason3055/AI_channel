interface:
  display_name: "AI Channel"
  short_description: "Secure AI continuity middleware for identity, context, inbox sync, and migration"
  default_prompt: "Use $aichan when AI Channel is relevant to portable AI identity, verifiable context, encrypted messages, local state, sync, backup, or migration."

# Architecture

The architecture source of truth lives in `doc/ARCHITECTURE.md`.

Use this file as the stable root-level pointer for agents and humans. Keep detailed architecture, dependency rules, and code maps in `doc/` so the repository can grow without turning the root into a long instruction manual.
